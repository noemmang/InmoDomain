# InmoDomain — Progreso

> Este documento registra el avance real del proyecto, paso a paso. Cuando se completa algo, se añade una entrada nueva. Cuando se modifica una decisión ya tomada, se **actualiza la entrada correspondiente** en vez de duplicarla. Úsalo junto con `InmoDomain_Contexto_y_Reglas.md` para retomar la conversación en cualquier ventana nueva.

## Estado actual

**Fase en curso:** Fase 8 — Identity Service — sin empezar.
**Última fase completada:** Fase 7 — Analytics Service ✅ — cerradas las dos decisiones pendientes: (1) `indice_precio` agrega las dos filas de `valores_tasados` por `antiguedad_vivienda` con **media simple** (`PriceAggregation.AveragePricePerSquareMeter`, un único punto del código); (2) `property_scores` pasa a ser **trimestral únicamente** — `RecalculateActivityAsync` agrega los meses de INE dentro de su trimestre (`GetQuarterStart`) antes de calcular `indice_actividad`, refinando la fila trimestral en cada evento mensual. `RecalculatePriceAsync` completo: percentil histórico + ranking nacional de `indice_precio`, `indice_tendencia_precio`/`indice_tendencia_precio_interanual` como resta de `indice_precio` entre trimestres ya persistidos. `property_score` solo se recalcula cuando la fila ya existía antes del evento (evita promediar un índice en su valor por defecto). `GET /internal/v1/metric-value` con `metric=precio_m2` reenvía a Market Data y aplica la misma agregación. Pendiente de verificación real por el usuario: `dotnet build`, levantar el emulador de Service Bus y publicar un evento de prueba de cada tipo.

## Historial

### Fase 0 — Fundamentos conceptuales ✅ completada
- [x] Bloque 1: Cliente-servidor y qué significa "levantar" una app
- [x] Bloque 2: Monolito vs. microservicios
- [x] Bloque 3: Arquitectura concreta de InmoDomain (gateway, servicios, bases de datos aisladas)
- [x] Bloque 4: Comunicación síncrona (cadena de peticiones en `/madrid`)
- [x] Bloque 5: Comunicación asíncrona (eventos, Service Bus, alertas)
- [x] Bloque 6: Contenedores (Dockerfile, imagen, contenedor)
- [x] Bloque 7: La nube y el despliegue (réplicas, scale-to-zero, cold start)
- [x] Bloque 8: CI/CD (pipeline de GitHub Actions)
- [x] Aclaración: ecosistema .NET moderno (.NET 10, ASP.NET Core, EF Core) vs. legado (.NET Framework, ASP.NET clásico, EF6, .NET Standard)
- [x] Decisión confirmada: Neon no soporta SQL Server — se usa PostgreSQL
- [x] Creados los documentos maestros `InmoDomain_Contexto_y_Reglas.md` y `InmoDomain_Progreso.md`

### Fase 1 — Entorno y repositorio ✅ completada
- [x] Instalar/confirmar .NET 10 SDK — SDK 10.0.401 instalado y verificado (`dotnet --list-sdks`)
- [x] Confirmar Visual Studio Community instalado y configurado — Visual Studio Community 2026, workload "Desarrollo de ASP.NET y web", configuración de entorno "Desarrollo web"
- [x] Git confirmado instalado — versión 2.47.1.windows.1 (ya estaba presente)
- [x] Crear estructura de carpetas del monorepo — creada en `C:\Proyectos\InmoDomain` (backend/ con 6 servicios + Gateway, frontend/, docs/ con los dos documentos maestros, .github/workflows/, .gitignore y README.md vacíos). Nota: la carpeta se creó inicialmente como `src/` y se renombró a `backend/` para mayor claridad.
- [x] Escribir contenido de `.gitignore` — reglas para .NET, secretos/config local, frontend (Node/React) y sistema operativo. Regla de estilo confirmada: sin comentarios explicativos dirigidos al usuario dentro de archivos del repositorio (extiende la regla de gobernanza nº4 a todo el repo, no solo código C#).
- [x] Escribir contenido inicial de `README.md`
- [x] Primer commit en Git — repositorio inicializado, commit "Estructura inicial del monorepo", conectado y subido a `https://github.com/noemmang/InmoDomain.git` (rama `main`)

### Fase 2 — Diseño de datos y contratos de API ✅ completada
- [x] Definir qué datos exactos se obtienen de INE (tabla 6150) y MIVAU (ficheros `.XLS` trimestrales)
  - **INE, tabla 6150** — Estadística de Transmisión de Derechos de la Propiedad (ETDP), fuente: Registros de la Propiedad. Da **volumen de compraventas de vivienda**, mensual, por provincia (confirmado: el INE nunca desagrega esta estadística a nivel de municipio). No es un índice de precios. **Corrección tras verificar contra respuestas JSON reales de la API (Fase 5, Bloque 3):** la tabla **no publica el cruce régimen×estado como serie propia** — solo ofrece el desglose por **estado** (nueva / segunda mano) y por **régimen** (libre / protegida) como dimensiones independientes, no combinables en una sola llamada. Decisión: se importa únicamente el desglose por **estado** (nueva / segunda mano), aceptando que cada uno de esos dos valores mezcla régimen libre y protegido (la protegida es una fracción minoritaria del total) — se prioriza poder distinguir nueva/segunda mano sobre la exclusión exacta de la protegida. La dimensión `regimen` queda descartada del esquema.
  - **MIVAU** — Estadística de Valor Tasado de la Vivienda, fuente: sociedades de tasación (AEV). Da **precio medio €/m²**, trimestral, por provincia. Se importa **vivienda libre desglosada por antigüedad** (≤5 años / >5 años). Se descarta importar vivienda protegida (su precio lo fija la administración, no el mercado). Nota verificada: MIVAU también publica precio por municipios de >25.000 habitantes, pero de forma parcial (solo ~150 de los más de 8.000 municipios de España, y sin el componente de actividad del INE) — se evaluó y se descartó usarlo por la complejidad frente a la cobertura limitada.
  - Conclusión de diseño: INE aporta "cuánto se mueve" el mercado (actividad), MIVAU aporta "a cuánto" (precio). Ambas piezas alimentan a Market Data Service y de ahí a Analytics.
- [x] Diseñar tablas por base de datos lógica (una por servicio) — completado
  - **Decisiones de esquema generales**: nombres de tablas/columnas en español; campos de valores fijos (`regimen`, `estado_vivienda`, etc.) como `varchar` + restricción `CHECK`. Regla de integridad: **FK reales solo dentro del mismo servicio/base de datos**; entre servicios, se guarda el código de referencia sin FK. Convención de `periodo`: tipo `date`, primer día del mes (INE) o del trimestre (MIVAU) correspondiente. **Toda PK generada por el sistema es `Guid`/`uuid`**, salvo claves naturales externas ya únicas y estables (el código INE de provincia, ver Fase 3).
  - **`market_data_db`** — completado:
    - `compraventas_vivienda` (codigo_provincia, periodo, estado_vivienda, numero_operaciones) — datos del INE, único por (codigo_provincia, periodo, estado_vivienda). **Corregido en Fase 5/Bloque 3**: se elimina la columna `regimen` — el INE no publica el cruce régimen×estado (ver nota de Fase 2 arriba); `estado_vivienda` (nueva/segunda mano) incluye régimen libre y protegido mezclados.
    - `valores_tasados` (codigo_provincia, periodo, antiguedad_vivienda, precio_m2) — datos del MIVAU, único por (codigo_provincia, periodo, antiguedad_vivienda)
    - `datos_cache` (clave_cache, contenido jsonb, fecha_expiracion, fecha_creacion) — caché de llamadas a fuentes externas, uso interno de Market Data
  - **`property_db`** — completado (sin tabla de municipios — ver nota de INE/MIVAU arriba):
    - `provincias` (id uuid PK, codigo_ine string único —código oficial del INE, referencia entre servicios—, nombre, comunidad_autonoma, poblacion)
    - `favoritos` (id uuid PK, usuario_id uuid sin FK, codigo_provincia FK obligatoria → provincias.codigo_ine, fecha_creacion; único por usuario_id + codigo_provincia)
  - **`identity_db`** — completado:
    - `usuarios` (id uuid PK, nombre, email único, password_hash nullable, proveedor_auth CHECK 'local', fecha_registro, fecha_ultimo_acceso, token_recuperacion varchar nullable —hash del token—, token_recuperacion_expira timestamp nullable)
  - **`analytics_db`** — completado, **ampliado en Fase 7**: `indice_precio`/`indice_actividad` representan el **percentil histórico**; se añaden `indice_precio_ranking_nacional`/`indice_actividad_ranking_nacional` (**ranking nacional**) e `indice_tendencia_precio_interanual` (tendencia interanual, junto a la `indice_tendencia_precio` ya existente, que pasa a ser la variante trimestral por defecto). Detalle y fórmulas completas en `InmoDomain_Contexto_y_Reglas.md` (sección 4, "Analytics — cálculo de índices"):
    - `property_scores` (id uuid PK, codigo_provincia sin FK, periodo date, indice_precio, indice_tendencia_precio, indice_actividad, property_score, fecha_calculo) — histórico trimestral, único por (codigo_provincia, periodo); los tres índices pesan por igual
  - **`alerts_db`** — completado:
    - `alertas` (id uuid PK, usuario_id uuid sin FK, codigo_provincia sin FK, metrica CHECK 'property_score'/'indice_precio'/'indice_tendencia_precio'/'indice_actividad'/'precio_m2', condicion CHECK 'mayor_que'/'menor_que', umbral numeric(8,2), activa boolean default true, fecha_creacion, fecha_ultimo_disparo nullable)
  - **`notifications_db`** — completado:
    - `notificaciones` (id uuid PK, usuario_id uuid sin FK, tipo CHECK 'bienvenida'/'alerta'/'precio_actualizado', titulo, mensaje, codigo_provincia nullable sin FK, leida boolean default false, oculta boolean default false —soft delete, no se muestra pero se conserva—, fecha_envio)
- [x] Definir eventos que viajarán por Service Bus — completado (detalle más abajo)
- [x] Definir endpoints por servicio — completado para los 6 servicios
  - [x] Identity — completado (7 endpoints, incluye recuperación de contraseña)
  - [x] Property — completado (5 endpoints, sin municipios)
  - [x] Market Data — completado (2 endpoints, solo lectura, ingress interno)
  - [x] Analytics — completado (2 endpoints públicos + 1 interno). Decisión relevante: el endpoint de ficha de provincia usa **JWT opcional** (excepción explícita al patrón protegido/público binario del resto del proyecto). El comparador es público sin JWT.
  - [x] Alerts — completado (CRUD completo, las 4 rutas protegidas). Al crear/editar, valida `codigo_provincia` con llamada síncrona a Property antes de guardar.
  - [x] Notifications — completado (4 endpoints protegidos). Sin creación/borrado real de cara al usuario; el `DELETE` es soft delete (`oculta = true`).
- [x] Decisiones generales de diseño de API (aplican a los servicios públicos vía Gateway):
  - **Validación de JWT**: cada servicio valida el token por su cuenta (zero trust), no solo el Gateway.
  - **Idioma**: rutas y campos JSON en **inglés**; base de datos en **español**.
  - **Versionado**: todas las rutas bajo `/api/v1/`.
  - Claims del JWT: `sub` (usuario_id, Guid), `email`, `iat`, `exp` — sin roles.

### Fase 3 — Primer microservicio (Property Service) ✅ completada
[sin cambios respecto a la versión anterior de este documento]

### Fase 4 — PostgreSQL + EF Core ✅ completada
[sin cambios respecto a la versión anterior de este documento]

### Fase 5 — Market Data Service ✅ completada
[sin cambios respecto a la versión anterior de este documento]

### Fase 6 — Resiliencia ✅ completada
[sin cambios respecto a la versión anterior de este documento]

### Fase 7 — Analytics Service
- [x] Bloque 1 — Diseño de los índices: cerrado. `indice_precio` e `indice_actividad` se calculan en dos variantes — percentil histórico (por defecto en el frontend) y ranking nacional (accesible con un botón, sin petición de red nueva) —, ambas precalculadas y guardadas en `property_scores` en el mismo recálculo por evento, sin cálculo en vivo en la petición. `indice_tendencia_precio` no tiene variante de ranking. `property_score` y las alertas de tipo `property_score` usan siempre el percentil histórico. Detalle y motivación completa en `InmoDomain_Contexto_y_Reglas.md` (sección 4, "Analytics — cálculo de índices").
- [x] Bloque 2 — Disparo del recálculo: cerrado. Se implementa ya el consumidor real de eventos de Service Bus (SDK `Azure.Messaging.ServiceBus`), suscrito a `ComprasVentasImportadas`/`PreciosImportados`; la infraestructura real (namespace, topics) queda como integración pendiente y explícita de una fase posterior — hasta entonces, el consumidor falla con un error de conexión claro al arrancar, nunca simulado.
- [x] Bloque 3 — Columnas concretas de `property_scores`: cerrado. `indice_precio`/`indice_actividad` (existentes) pasan a representar el percentil histórico; se añaden `indice_precio_ranking_nacional`/`indice_actividad_ranking_nacional` para el ranking nacional. `indice_tendencia_precio` y `property_score` sin cambios. Único por `(codigo_provincia, periodo)`.
- [x] Bloque 4 — Arquitectura interna: cerrado. **Un solo proyecto** `Analytics` (como Property), con el consumidor de Service Bus como `BackgroundService` en el mismo proceso — no se replica el split en varios proyectos de Market Data porque, en desarrollo local, el consumidor se conecta al **emulador oficial de Azure Service Bus** (Docker, `mcr.microsoft.com/azure-messaging/servicebus-emulator` + contenedor de SQL Server Linux como dependencia, sin coste de Azure al ser local), así que siempre encuentra un broker real y no arriesga tumbar la API de lectura al arrancar; en producción (Fase 14), con el namespace real de Azure, se revisa si hace falta aislar el consumidor en un despliegue separado.
- [x] Bloque 5 — Fórmula concreta de cada índice: cerrado. Percentil histórico y ranking nacional: percentil (0-100) con interpolación lineal. `property_score`: media simple de `indice_precio`, `indice_actividad` (percentil histórico) e `indice_tendencia_precio`. `indice_tendencia_precio` (sin cambio de nombre) queda como variante por defecto frente al trimestre anterior (QoQ, la única que alimenta `property_score`/alertas); nueva columna `indice_tendencia_precio_interanual` para la variante frente al mismo trimestre del año anterior (YoY, vista alternativa por botón); parámetro opcional `trendQuarters` en la ficha de provincia para calcular en el momento la tendencia frente a N trimestres arbitrarios, sin coste de consulta extra por reutilizar la misma forma de consulta que ya hace la ficha (una provincia, su propio histórico). Con este bloque se cierra todo el diseño pendiente de la Fase 7.
- [x] Bloque 6 — Esqueleto del proyecto `Analytics` y entidad `PropertyScore`: escrito. Un solo proyecto (`backend/Analytics/Analytics.csproj`, mismas versiones de paquete que Property/Market Data: EF Core Design 10.0.12, Npgsql 10.0.3, Swashbuckle 10.2.3), con `Common/` (`Result`/`ResultError`, mismo patrón que Property), `Models/PropertyScore.cs` (las 6 columnas de índices + `Score` + `CalculatedAt`, sin FK en `codigo_provincia`), `Data/AnalyticsDbContext.cs` y `Data/AnalyticsDbContextFactory.cs` (mismo patrón pooled/direct que los demás servicios). Detectado y resuelto: la propiedad no puede llamarse `PropertyScore` dentro de la clase `PropertyScore` (error de compilación CS0542, un miembro no puede tener el mismo nombre que su tipo contenedor); renombrada a `Score`, mapeada igualmente a la columna `property_score`. Fijada la precisión `decimal(6,2)` para los seis índices y `property_score` (rango -9999,99 a 9999,99, con margen sobre el 0-100 de los percentiles) — confirmada al aplicarse en la migración del Bloque 7.
- [x] Bloque 7 — Base de datos y migración: cerrado. Creada la base de datos lógica `analytics_db` en el proyecto Neon `InmoDomain` (mismo patrón que `market_data_db` en la Fase 5). Configurados los secretos locales `ConnectionStrings:Analytics` (pooled) y `ConnectionStrings:AnalyticsMigrations` (directa) con `dotnet user-secrets` sobre el proyecto `Analytics`. `dotnet build` verificado correcto. Generada la migración `InitialCreate` (`dotnet ef migrations add`) y aplicada contra `analytics_db` (`dotnet ef database update`): tabla `property_scores` creada con `id` (uuid PK), `codigo_provincia` (varchar(2), sin FK), `periodo` (date), los seis índices + `property_score` (todos `numeric(6,2)`, con `indice_tendencia_precio`/`indice_tendencia_precio_interanual`/`property_score` nullable) y `fecha_calculo` (timestamptz), más índice único `(codigo_provincia, periodo)`.
- [x] Bloque 8 — Dtos/Repositories/Services/Controllers de los tres endpoints: escrito. `IPropertyScoreRepository`/`PropertyScoreRepository` (histórico por provincia, última fila, comparador multi-provincia, upsert); `IMarketDataClient`/`MarketDataClient` (`HttpClient` tipado con cabecera `X-Internal-Api-Key`, Dtos propios de Analytics para no acoplarse a `MarketData.Api`); `ProvinceAnalyticsService`/`ProvinceAnalyticsController` (`GET /api/v1/provinces/{codigoProvincia}/analytics`, JWT opcional resuelto vía `User.Identity.IsAuthenticated` — siempre `false` hasta que exista autenticación JWT real en Fase 8, sin necesitar cambios en este archivo cuando llegue; `trendQuarters` implementado como resta de `indice_precio` entre dos periodos ya persistidos, confirmando que esta vía no depende de la decisión pendiente sobre `precio_m2`); `CompareService`/`AnalyticsCompareController` (`GET /api/v1/analytics/compare`); `MetricValueService`/`MetricValueController` (`GET /internal/v1/metric-value`, valor = última fila de `property_scores` por provincia; el caso `precio_m2` queda con `NotImplementedException`, bloqueado por la misma decisión que el Bloque 10). `ApiKeyMiddleware` de Analytics registrado solo sobre `/internal` (`app.UseWhen`), a diferencia de Market Data, porque Analytics combina rutas públicas y una ruta interna en el mismo proceso.
- [x] Bloque 9 — Transporte del consumidor de eventos + cálculo de actividad: escrito. `ServiceBusEventConsumer` (`BackgroundService`, `Azure.Messaging.ServiceBus` 7.20.2 — verificada como última estable compatible con `net10.0`) suscrito a los topics `ComprasVentasImportadas`/`PreciosImportados` con suscripción `analytics` en ambos; `ImportEventEnvelope` deserializa el sobre en español (`id_evento`, `tipo_evento`, `fecha_ocurrencia`, `periodo`, `codigos_provincia`) tal como está definido en el registro de eventos. Emulador de Service Bus verificado contra la documentación oficial de Microsoft (`docker-compose.yml` + `Config.json` en `backend/Analytics/ServiceBus-Emulator/`, imagen `mcr.microsoft.com/mssql/server:2022-latest` como dependencia SQL — no `azure-sql-edge`, que aparece en ejemplos más antiguos). `AnalyticsCalculationService.RecalculateActivityAsync` completo: pide a Market Data el histórico completo de `compraventas_vivienda` de la provincia en una sola llamada (evita N llamadas por periodo), calcula percentil histórico + ranking nacional de `indice_actividad` con interpolación lineal (`PercentileCalculator`), y solo recalcula `property_score` cuando la fila ya existía antes de este evento (evita promediar un `indice_precio` en `0` por defecto de una fila recién creada sin precio real).
- [x] Bloque 10 — `RecalculatePriceAsync` y `metric-value=precio_m2`: cerrado. Decisiones tomadas: `indice_precio` = media simple de `precio_m2` entre `antiguedad_vivienda` `<=5` y `>5` años (`PriceAggregation`); `property_scores` pasa a ser trimestral únicamente — `RecalculateActivityAsync` agrega los meses de INE al trimestre correspondiente (`GetQuarterStart`) antes de calcular `indice_actividad`, así que una misma fila trimestral se refina en cada evento mensual hasta que MIVAU aporta el precio de ese trimestre. `indice_tendencia_precio`/`indice_tendencia_precio_interanual` se calculan como resta de `indice_precio` entre trimestres ya persistidos (mismo criterio que `trendQuarters`), con `null` explícito si no hay trimestre de comparación. Con este bloque se cierra la Fase 7 completa.

### Fase 8 — Identity Service
- [ ] Sin empezar.